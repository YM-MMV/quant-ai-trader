# Normalize line endings. Shell scripts MUST use LF or they break on Unix
# ("bad interpreter: /usr/bin/env bash\r"). PowerShell scripts use CRLF.
* text=auto eol=lf
*.sh text eol=lf
*.ps1 text eol=crlf
